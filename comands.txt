# Clean up previous build
docker-compose down -v

# Rebuild with no cache
docker-compose build --no-cache

# Start services
docker-compose up


🛠️ Step-by-Step Fix Process
1. Update Dockerfile
Replace backend/Dockerfile with the optimized version above.

2. Clean Everything
Bash

# Stop and remove all containers
docker-compose down -v

# Remove old images (optional but recommended)
docker system prune -a
3. Rebuild
Bash

# Build with no cache
docker-compose build --no-cache backend celery
4. Start Services
Bash

# Start all services
docker-compose up
5. Create Superuser (in new terminal)
Bash

docker-compose exec backend python manage.py createsuperuser


# Clean everything
docker-compose down -v
docker system prune -f

# Rebuild
docker-compose build --no-cache

# Start with logs
docker-compose up


#finding which service used my which service 
docker ps -a --filter ancestor=product-image-generator-frontend:latest

#remove container by its id
docker rm 056607588b92

docker rm -f productgen_db
# or equivalently
# docker rm -f d0c779483e07

stop container by its id 
docker stop <CONTAINER_ID>

changes apply steps:
docker-compose down
docker-compose build
docker-compose up

# 1. Stop all containers and remove the old "backend" container
docker-compose down --rmi local

# 2. Rebuild everything from scratch (This ensures your latest code is copied in)
docker-compose up --build

# 1. Apply the new migrations to the database
docker-compose exec backend python manage.py migrate

# 2. Restart the backend to ensure it loads everything fresh
docker-compose restart backend